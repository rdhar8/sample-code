name: Auto Fix
on:
  push:
    branches: [Nitin-test]
  pull_request:
    branches: [ main ]
jobs:
  smart-code-review: 
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-node@v1
      with:
        node-version: '16'
    - name: Install dependencies
      run: echo "Nitin"
  smart-code-review1: 
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-node@v1
      with:
        node-version: '16'
    - name: Install dependencies
      run: echo "Jain"
